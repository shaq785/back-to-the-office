var Enemy
//constructor

  //properties
    //x pos
    //y pos
    //sprite

  //method
     // update position
         //if enemy is not passed boundary
         // move foward increment x by speed * dt
         // else  reset pos to start


var Hero
 //constructor

   //properties
     //x pos
     //y pos
     //sprite

   //method
      // update position
          // check collision
            // did hero x and y collide with the enemy?If- Then
          // check victory
            // did hero touched the x and y top row of the grid? if then
      // render This method will draw or redraw our hero to the board every loop through the main game loop
          // draw hero sprite  x and y  position for every loop
      // handle
          //update player x and y according to the input
      // reset
          // set x and y  to start  - or because it happend the collision or because Hero wins

//Initializaing Objects
 // New Hero object
 // Init allEnemies Array
